# MLLM Fine-Tuning Minimal (Logs)

This folder contains the raw training records for the **MLLM minimal** fine tuning.

This configuration represents the most basic setup in our ablation study (Section 4.3), where the Qwen is fine-tuned exclusively on 
basic image-level anomaly labels without specific location priors or clustering information.
